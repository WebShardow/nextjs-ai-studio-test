# 🚀 Next.js App Router & Gemini AI Integration

## 💡 โครงการ: [Next.js 16 + AI Studio]

โครงการนี้คือตัวอย่างการผสานรวม **Google Gemini Generative AI** เข้ากับแอปพลิเคชัน Next.js โดยใช้ **App Router** และ **Server Component** เพื่อเรียกใช้ AI SDK อย่างปลอดภัยและมีประสิทธิภาพ

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

เรามุ่งเน้นการใช้งานเทคโนโลยีล่าสุดเพื่อให้ได้ประสิทธิภาพและความเสถียรสูงสุด:

| หมวดหมู่ | เทคโนโลยี | เวอร์ชัน | หมายเหตุ |
| :--- | :--- | :--- | :--- |
| **Framework** | Next.js | `16.0.6` | App Router & Server Components |
| **Styling** | Tailwind CSS | `^4` | ใช้ `@tailwindcss/postcss` |
| **TypeScript** | TypeScript | `^5` | การพัฒนาแบบ Strongly-Typed |
| **Generative AI**| Google Gemini AI SDK | `@ai-sdk/google` | เพื่อเรียกใช้ Gemini API |
| **Deployment** | Vercel | N/A | แพลตฟอร์มสำหรับ Hosting |

---

## 🔑 ข้อกำหนดเบื้องต้น (Prerequisites)

ก่อนเริ่มต้น คุณต้องมีสิ่งเหล่านี้ติดตั้งและตั้งค่า:

1. **Node.js** (v14+) และ **npm**
2. **คีย์ Gemini API** ที่ได้รับจาก Google AI Studio
3. ความคุ้นเคยเบื้องต้นกับ Next.js App Router และ JavaScript/TypeScript

---

## ⚙️ การตั้งค่าและการติดตั้ง (Setup & Installation)

ทำตามขั้นตอนเหล่านี้เพื่อรันโครงการบนเครื่องของคุณ:

### 1. โคลน Repository

```bash
git clone [https://github.com/WebShardow/nextjs-ai-studio.git](https://github.com/WebShardow/nextjs-ai-studio.git)
cd nextjs-ai-studio
````

### 2\. ติดตั้ง Dependencies

```bash
npm install
# หรือ
yarn install
```

### 3\. การตั้งค่าคีย์ API (สำคัญมาก)

เพื่อจัดการคีย์ API อย่างปลอดภัยตามหลักปฏิบัติที่ดีที่สุด ให้สร้างไฟล์ชื่อ **`.env.local`** ที่รูทของโปรเจกต์ และใส่ตัวแปรสภาพแวดล้อมดังนี้ :

```env
# คีย์นี้จะถูกโหลดโดย Next.js และ AI SDK โดยอัตโนมัติ
GOOGLE_GENERATIVE_AI_API_KEY="YOUR_GEMINI_API_KEY_HERE"
```

> ⚠️ **คำเตือน :** ห้าม Commit ไฟล์ `.env.local` เข้า GitHub โดยเด็ดขาด

### 4\. รัน Development Server

รัน Development Server โดยใช้ Turbopack (ตามค่าเริ่มต้นของ Next.js 16.0.6) :

```bash
npm run dev
```

แอปพลิเคชันจะเปิดที่ `http://localhost:3000` (หรือพอร์ตที่ว่าง)

---

## 💻 วิธีการใช้งาน Gemini API ในโค้ด

การเรียกใช้ Gemini API ถูกดำเนินการใน **Server Component** (`app/page.tsx`) เพื่อซ่อนคีย์ API จากฝั่ง Client (เบราว์เซอร์)

### โค้ดตัวอย่าง (`app/page.tsx`)

```typescript
import { google } from "@ai-sdk/google";
import { generateText } from "ai";

export default async function Home() {
    // SDK จะค้นหา GOOGLE_GENERATIVE_AI_API_KEY โดยอัตโนมัติ
    const { text } = await generateText({
        model: google("gemini-2.5-flash"),
        prompt: "เขียนหัวข้อสำหรับบทความ Next.js App Router และ Gemini API",
    });

    return (
        <main>
            <h1>ผลลัพธ์จาก Gemini:</h1>
            <p>{text}</p>
        </main>
    );
}
```

---

## ☁️ การปรับใช้บน Vercel (Deployment)

เนื่องจากโครงการนี้ถูกออกแบบมาเพื่อปรับใช้บน **Vercel** การตั้งค่าจึงง่ายมาก :

1. เชื่อมต่อ Repository ของคุณเข้ากับ Vercel
2. **สำคัญ :** ตั้งค่า **Environment Variable** ชื่อ `GOOGLE_GENERATIVE_AI_API_KEY` ใน Vercel Dashboard สำหรับสภาพแวดล้อม Production และ Preview เพื่อให้แอปพลิเคชันทำงานได้

---

## 🤝 การมีส่วนร่วม (Contributing)

เรายินดีรับการมีส่วนร่วมในรูปแบบ Pull Requests\! หากคุณต้องการปรับปรุงโครงการ:

1. Fork โครงการนี้
2. สร้าง Branch ใหม่ (`git checkout -b feature/AmazingFeature`)
3. Commit การเปลี่ยนแปลงของคุณ (`git commit -m 'Add some AmazingFeature'`)
4. Push ไปยัง Branch นั้น (`git push origin feature/AmazingFeature`)
5. เปิด Pull Request

---

## 👤 ผู้พัฒนา

โครงการนี้พัฒนาโดย :

**[WebShardow/Microtronic Co.,Ltd.]**

* **GitHub :** **โปรไฟล์ GitHub ของฉัน** [GitHub Developer Program Member](https://github.com/WebShardow)
* **Google Developers Profile :** [Developers.Google.com](https://developers.google.com/profile/u/102913520173409330775) (ในฐานะตัวแทนจำหน่าย Google Workspace)

---
