# 🔑 GOOGLE\_GENERATIVE\_AI\_API\_KEY หมายถึงอะไร?

* **Google Generative AI Provider :** หมายถึงบริการ AI สร้างสรรค์ (Generative AI) ของ Google ซึ่งปัจจุบันคือ **Gemini API**
* **API Key (คีย์ API) :** เป็นเหมือนรหัสผ่านเฉพาะตัวที่ใช้ในการ **ระบุตัวตน** (Authentication) ของคุณในฐานะผู้ใช้ และ **อนุญาต** (Authorization) ให้แอปพลิเคชันของคุณเข้าถึงและเรียกใช้บริการ AI ของ Google ได้
* **GOOGLE\_GENERATIVE\_AI\_API\_KEY :** เป็น **ชื่อมาตรฐานของตัวแปรสภาพแวดล้อม** ที่ไลบรารีของ Google (เช่น SDK สำหรับ Python, Node.js ฯลฯ) ถูกตั้งค่าไว้ให้ **ค้นหาคีย์ API โดยอัตโนมัติ** เมื่อคุณเรียกใช้ฟังก์ชัน API

> **สรุป :** เมื่อคุณตั้งค่าตัวแปรนี้แล้ว คุณไม่จำเป็นต้องระบุคีย์ API ในโค้ดทุกครั้งที่เรียกใช้ API ซึ่งเป็น **หลักปฏิบัติที่ดีด้านความปลอดภัย** (Security Best Practice)

-----

## 🛠️ วิธีการตั้งค่าใน macOS/Linux

นี่คือขั้นตอนการใช้งานคำสั่งที่คุณให้มา:

### ขั้นตอนที่ 1 : รับคีย์ API ของคุณ

ก่อนอื่นคุณต้องสร้างคีย์ API ของคุณ หากคุณยังไม่มี:

1. ไปที่ **Google AI Studio** หรือหน้า **Gemini API Key**
2. สร้างคีย์ใหม่และ **คัดลอกคีย์นั้นทันที** (คีย์จะแสดงเพียงครั้งเดียว)

### ขั้นตอนที่ 2 : ตั้งค่าตัวแปรสภาพแวดล้อม

คุณสามารถใช้คำสั่งที่คุณให้มาใน **Terminal** (ใน macOS/Linux) เพื่อตั้งค่าตัวแปรนี้สำหรับเซสชันปัจจุบัน

**คำสั่ง :**

```bash
export GOOGLE_GENERATIVE_AI_API_KEY="YOUR_API_KEY_HERE"
```

> ⚠️ **สิ่งที่คุณต้องทำ :** **แทนที่** `YOUR_API_KEY_HERE` ด้วยคีย์จริงที่คุณได้รับมา (เช่น `AIzaSy...`).

**ตัวอย่างการใช้งาน :**

```bash
export GOOGLE_GENERATIVE_AI_API_KEY="AIzaSyB_1234567890abcdefghijklmnopqrstuvw"
```

### ขั้นตอนที่ 3: ตรวจสอบการตั้งค่า (ไม่บังคับ)

คุณสามารถตรวจสอบว่าตัวแปรถูกตั้งค่าแล้วหรือไม่โดยใช้คำสั่ง `echo`:

```bash
echo $GOOGLE_GENERATIVE_AI_API_KEY
```

> **ผลลัพธ์:** ควรแสดงคีย์ API ของคุณออกมา

### ข้อควรทราบที่สำคัญ (ความต่อเนื่องของตัวแปร)

คำสั่ง `export` จะตั้งค่าตัวแปรสภาพแวดล้อม **เฉพาะในเซสชัน Terminal ปัจจุบันเท่านั้น**

* หากคุณ **ปิด Terminal** หรือ **เปิด Terminal ใหม่** ตัวแปรนี้จะ **หายไป**
* **สำหรับนักพัฒนา (DevOps / GitHub Developer Program):** เพื่อให้ตัวแปรนี้ **อยู่ถาวร** คุณควรเพิ่มคำสั่ง `export` นี้ลงในไฟล์เริ่มต้นของ Shell ที่คุณใช้ (เช่น `~/.bashrc`, `~/.zshrc`, หรือ `~/.profile`)

**วิธีทำให้ถาวร (สำหรับ Zsh หรือ Bash) :**

1. เปิดไฟล์กำหนดค่า Shell ของคุณ (เช่น `nano ~/.zshrc` หรือ `nano ~/.bashrc`)
2. เพิ่มบรรทัดนี้ที่ท้ายไฟล์ :

    ```bash
    export GOOGLE_GENERATIVE_AI_API_KEY="YOUR_API_KEY_HERE"
    ```

3. บันทึกและปิดไฟล์
4. โหลดการกำหนดค่าใหม่: `source ~/.zshrc` (หรือไฟล์ที่คุณแก้ไข)

-----

## ☁️ การใช้งานบน Vercel (เพิ่มเติม)

เนื่องจากคุณใช้งาน **Vercel** สำหรับการปรับใช้ (Deployment) คุณจะต้องตั้งค่าคีย์ API ในรูปแบบของ **Environment Variables ของ Vercel** ด้วย:

1. ไปที่หน้า **Project Dashboard** ของโปรเจกต์ Next.js ของคุณบน Vercel
2. คลิกที่แท็บ **Settings** \> **Environment Variables**
3. เพิ่มตัวแปรใหม่:
      * **Name:** `GOOGLE_GENERATIVE_AI_API_KEY`
      * **Value:** คีย์ API จริงของคุณ
      * **Environments:** เลือก **Development, Preview, และ Production** (ตามความเหมาะสม)
4. บันทึก

การทำเช่นนี้จะทำให้โค้ด Next.js ที่รันอยู่บน Vercel สามารถเข้าถึงคีย์ API ได้อย่างปลอดภัยครับ

-----
